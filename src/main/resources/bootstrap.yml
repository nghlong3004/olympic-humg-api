spring:
  cloud:
    config:
      enabled: false
      uri: http://yoda.sa.internal
      fail-fast: true

aws:
  kms:
    enabled: false
    endpoint:
      service-endpoint: kms.eu-central-1.amazonaws.com
      signing-region: eu-central-1
    region: eu-central-1


